{%- liquid
  assign theme_rtl = false
  if settings.enable_rtl and settings.langauges_rtl == blank
    assign theme_rtl = true
  endif

  if settings.enable_rtl and settings.langauges_rtl != blank
    assign rtl_languages = settings.langauges_rtl | split: ','
    for language in rtl_languages
      if language == request.locale.iso_code
        assign theme_rtl = true
      endif
    endfor
  endif
-%}
{{ 'section-product-tabs.css' | asset_url | stylesheet_tag }}
{{ 'component-rating.css' | asset_url | stylesheet_tag }}
{{ 'countdown-timer.css' | asset_url | stylesheet_tag }}
{{ 'stock-countdwon.css' | asset_url | stylesheet_tag }}
{{ 'product-tooltip.css' | asset_url | stylesheet_tag }}
{{ 'section-title.css' | asset_url | stylesheet_tag }}
{{ 'product-card.css' | asset_url | stylesheet_tag }}

{%- comment -%}
  {{ 'component-price.css' | asset_url | stylesheet_tag }}
{%- endcomment -%}

<style>
  .rating {
    display: inline-block;
    margin: 0;
  }

  .product .rating-star {
    --letter-spacing: 0.8;
    --font-size: 2;
  }

  .product__card .rating-star {
    --letter-spacing: 0.7;
    --font-size: 2;
  }

  .rating-star {
    --percent: calc(
      (
          var(--rating) / var(--rating-max) + var(--rating-decimal) *
            var(--font-size) /
            (var(--rating-max) * (var(--letter-spacing) + var(--font-size)))
        ) * 100%
    );
    letter-spacing: calc(var(--letter-spacing) * 1rem);
    font-size: calc(var(--font-size) * 1rem);
    line-height: 1;
    display: inline-block;
    font-family: Times;
    margin: 0;
  }

  .rating-star::before {
    content: '★★★★★';
    background: linear-gradient(
      90deg,
      var(--color-icon) var(--percent),
      rgba(var(--color-foreground), 0.15) var(--percent)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .rating-text {
    display: none;
  }

  .rating-count {
    display: inline-block;
    margin: 0;
  }

  @média (forced-colors: active) {
    .rating {
      display: none;
    }

    .rating-text {
      display: block;
    }
  }
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.mobile_padding_top }}px;
      padding-bottom: {{ section.settings.mobile_padding_bottom }}px;
    }
    @média screen and (min-width: 750px) {
      .section-{{ section.id }}-padding {
        padding-top: {{ section.settings.padding_top }}px;
        padding-bottom: {{ section.settings.padding_bottom }}px;
      }
    }
</style>

{% if theme_rtl %}
  {{ 'section-product-tabs-rtl.css' | asset_url | stylesheet_tag }}
  {{ 'product-card-rtl.css' | asset_url | stylesheet_tag }}
{% endif %}

{%- liquid
  assign container = ''
  if section.settings.container == 'container'
    assign container = 'container'
  elsif section.settings.container == 'container-fluid'
    assign container = 'container-fluid'
  else
    assign container = 'container-fluid px-0'
  endif

  assign flex_align = 'justify-content-center'
  if section.settings.text_center == 'left'
    assign flex_align = 'justify-content-start'
  elsif section.settings.text_center == 'right'
    assign flex_align = 'justify-content-end'
  endif
-%}

<div
  class="product__section section-{{ section.id }}-padding section-products--tabs"
  data-section-id="{{ section.id }}"
  data-section-type="product-tab"
>
  <div class="{{ container }}">
    {% if section.settings.heading != blank or section.settings.subtitle != blank %}
      <div class="section-heading text-{{ section.settings.text_center }}  {% if section.settings.border_line or section.settings.show_navigation_topbar %} mb-50 {% else %} mb-30{% endif %}">
        {% if section.settings.heading_position == 'bottom' and section.settings.subtitle != blank %}
          <span class="section-heading__sub_title">{{- section.settings.subtitle -}}</span>
        {% endif %}
        <h2 class="section-heading__title {% if section.settings.heading_position == "top" and section.settings.subtitle != blank and section.settings.border_line %}heading__space--bottom{% endif %} ">
          {% if section.settings.border_line %}<span>{% endif %}
          {{- section.settings.heading -}}
          {% if section.settings.border_line %}</span>{% endif %}
        </h2>
        {% if section.settings.heading_position == 'top' and section.settings.subtitle != blank %}
          <span class="section-heading__sub_title">{{- section.settings.subtitle -}}</span>
        {% endif %}
      </div>
    {% endif %}

    <ul class="product__tab--one product__tab--btn d-flex {{ flex_align }} mb-35">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'product_tab' -%}
            <li
              class="product__tab--btn__list {% if forloop.first == true %}active{% endif %}"
              data-toggle="tab"
              data-target="#tab__target--{{ block.id }}"
              {{ block.shopify_attributes }}
            >
              {{ block.settings.title }}
            </li>
        {%- endcase -%}
      {%- endfor -%}
    </ul>
    <div class="tab_content">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'product_tab' -%}
            {%- liquid
              assign featured_product_limit = block.settings.products_to_show
              assign collection = collections[block.settings.collection]

              assign more_in_collection = false
              if collection.all_products_count > featured_product_limit
                assign more_in_collection = true
              endif

              assign horizontal_card_layout = false
              if section.settings.card_layout == 'horizontal'
                assign horizontal_card_layout = true
              endif
            -%}
            <div
              id="tab__target--{{ block.id }}"
              class="tab_pane {% if forloop.first == true %}active show{% endif %}"
              {{ block.shopify_attributes }}
            >
              <div class="product__section--inner">
                <div class="row {% unless horizontal_card_layout %}row-cols-xxl-{{ section.settings.product_column }} row-cols-xl-{{ section.settings.product_column }} row-cols-lg-{{ section.settings.product_column_laptop }} row-cols-md-{{ section.settings.product_column_tablet }} row-cols-{{ section.settings.product_column_mobile }}{% endunless %}">
                  {%- for product in collection.products limit: featured_product_limit -%}
                    <div class="{% if horizontal_card_layout %}col-12 col-md-6 mb-20{% else %}col mb-30{% endif %}">
                      {% if horizontal_card_layout %}
                        {% render 'product-card-horizontal',
                          product_card_product: product,
                          show_cart_button: section.settings.show_cart_button,
                          show_preorder_button: section.settings.show_preorder_button,
                          show_title: section.settings.show_title,
                          show_price: section.settings.show_price,
                          color_scheme: section.settings.product_card_color_scheme,
                          corner_radius: section.settings.product_card_radius,
                          inventory_status: section.settings.inventory_status,
                          free_shipping_threshold: section.settings.horizontal_free_shipping_threshold,
                          free_shipping_label: section.settings.horizontal_free_shipping_label,
                          sale_badge_label: section.settings.horizontal_sale_badge_label
                        %}
                      {% else %}
                        {% render 'product-card',
                          product_card_product: product,
                          média_size: section.settings.image_ratio,
                          show_secondary_image: section.settings.show_secondary_image,
                          show_vendor: section.settings.show_vendor,
                          show_badge: section.settings.show_badges,
                          show_cart_button: section.settings.show_cart_button,
                          show_preorder_button: section.settings.show_preorder_button,
                          show_quick_view: section.settings.show_quick_view_button,
                          show_quick_compare: section.settings.show_compare_view_button,
                          show_wishlist: section.settings.show_wishlist_button,
                          show_countdown: section.settings.show_countdown,
                          show_title: section.settings.show_title,
                          show_price: section.settings.show_price,
                          show_rating: section.settings.show_product_rating,
                          card_style: section.settings.card_style,
                          color_swatches: section.settings.color_swatches,
                          color_scheme: section.settings.product_card_color_scheme,
                          spacing: section.settings.product_card_spacing,
                          corner_radius: section.settings.product_card_radius,
                          inventory_status: section.settings.inventory_status
                        %}
                      {% endif %}
                    </div>
                  {%- else -%}
                    {%- assign a = 1 -%}
                    {%- for i in (1..featured_product_limit) -%}
                      {%- liquid
                        assign product_item = 'product-' | append: a
                        assign a = a | plus: 1
                      -%}
                      {%- render 'product-card-placeholder', product_item: product_item, productItem: 'col mb-30' -%}
                      {%- liquid
                        if a == 7
                          assign a = 1
                        endif
                      -%}
                    {%- endfor -%}
                  {%- endfor -%}
                </div>

                {% liquid
                  if section.block.button_style == 'primary'
                    assign button_class = ''
                  else
                    assign button_class = 'button--secondary'
                  endif
                %}

                {%- if block.settings.show_view_all and more_in_collection and block.settings.button_label != blank -%}
                  <div class="collection--button text-center mt-20">
                    <a
                      href="{{ collection.url }}"
                      class="button button--{{ block.settings.button_size }} {{ button_class }}  {% if block.settings.button_icon %} button--with-icon{% endif %}"
                    >
                      {{- block.settings.button_label -}}

                      {% if block.settings.button_icon %}
                        <span class="button--icon button--icon-right"> {% render 'icon-arrow-right' %} </span>
                      {% endif %}
                    </a>
                  </div>
                {%- endif -%}
              </div>
            </div>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Abas de coleções",
  "settings": [
    {
      "type": "header",
      "content": "Geral"
    },
    {
      "type": "select",
      "id": "container",
      "label": "Largura da página",
      "default": "container-fluid",
      "options": [
        {
          "value": "container",
          "label": "Limitado"
        },
        {
          "value": "container-fluid",
          "label": "Largura total"
        }
      ]
    },
    {
      "type": "header",
      "content": "Cabeçalho da seção"
    },
    {
      "type": "inline_richtext",
      "id": "subtitle",
      "default": "Subtitle",
      "label": "Subtítulo"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "Product Tabs",
      "label": "t:sections.featured-collection.settings.title.label"
    },
    {
      "type": "select",
      "id": "heading_position",
      "options": [
        {
          "value": "top",
          "label": "Topo"
        },
        {
          "value": "bottom",
          "label": "Inferior"
        }
      ],
      "default": "bottom",
      "label": "Posição do título"
    },
    {
      "type": "select",
      "id": "text_center",
      "options": [
        {
          "value": "right",
          "label": "Direita"
        },
        {
          "value": "left",
          "label": "Esquerda"
        },
        {
          "value": "center",
          "label": "Centro"
        }
      ],
      "default": "center",
      "label": "Alinhamento"
    },
    {
      "type": "checkbox",
      "id": "border_line",
      "label": "Ativar linha de borda",
      "default": true
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "product_column",
      "label": "Número de colunas no desktop",
      "options": [
        {
          "value": "3",
          "label": "3"
        },
        {
          "value": "4",
          "label": "4"
        },
        {
          "value": "5",
          "label": "5"
        }
      ],
      "default": "5"
    },
    {
      "type": "select",
      "id": "product_column_laptop",
      "label": "Número de colunas no notebook",
      "options": [
        {
          "value": "2",
          "label": "2"
        },
        {
          "value": "3",
          "label": "3"
        },
        {
          "value": "4",
          "label": "4"
        }
      ],
      "default": "4"
    },
    {
      "type": "select",
      "id": "product_column_tablet",
      "label": "Número de colunas no tablet",
      "options": [
        {
          "value": "2",
          "label": "2"
        },
        {
          "value": "3",
          "label": "3"
        },
        {
          "value": "4",
          "label": "4"
        }
      ],
      "default": "3"
    },
    {
      "type": "select",
      "id": "product_column_mobile",
      "label": "Número de colunas no mobile",
      "options": [
        {
          "value": "1",
          "label": "1"
        },
        {
          "value": "2",
          "label": "2"
        }
      ],
      "default": "2"
    },
    {
      "type": "header",
      "content": "Card de produto"
    },
    {
      "type": "select",
      "id": "card_layout",
      "label": "Layout do card",
      "options": [
        {
          "value": "grid",
          "label": "Grade"
        },
        {
          "value": "horizontal",
          "label": "Horizontal"
        }
      ],
      "default": "grid"
    },
    {
      "type": "select",
      "id": "card_style",
      "label": "Estilo do card",
      "options": [
        {
          "value": "style_1",
          "label": "Estilo 1"
        },
        {
          "value": "style_2",
          "label": "Estilo 2"
        }
      ],
      "default": "style_2"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__3.label"
        },
        {
          "value": "landscape",
          "label": "Paisagem"
        }
      ],
      "default": "adapt",
      "label": "t:sections.main-collection-product-grid.settings.image_ratio.label"
    },
    {
      "type": "checkbox",
      "id": "show_secondary_image",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_secondary_image.label"
    },
    {
      "type": "checkbox",
      "id": "color_swatches",
      "default": true,
      "label": "Ativar amostras de cor",
      "info": "Para exibir amostras de cor, ative está opção. [Saiba mais](https://themihub.gitbook.io/suruchi-theme/theme-settings/color-swatches)."
    },
    {
      "type": "checkbox",
      "id": "show_badges",
      "default": true,
      "label": "Mostrar selos"
    },
    {
      "type": "checkbox",
      "id": "show_cart_button",
      "default": true,
      "label": "Mostrar botão do carrinho"
    },
    {
      "type": "checkbox",
      "id": "show_preorder_button",
      "default": true,
      "label": "Mostrar botão de pré-venda",
      "info": "Permite que clientes comprem itens fora de estoque. [Saiba mais](https://themihub.gitbook.io/suruchi-theme/general-topics-faq/how-to-enable-the-pre-order-product-to-your-store)"
    },
    {
      "type": "checkbox",
      "id": "show_quick_view_button",
      "default": true,
      "label": "Mostrar visualização rápida"
    },
    {
      "type": "checkbox",
      "id": "show_compare_view_button",
      "default": true,
      "label": "Mostrar botão de comparar"
    },
    {
      "type": "checkbox",
      "id": "show_wishlist_button",
      "default": true,
      "label": "Mostrar botão de favoritos"
    },
    {
      "type": "checkbox",
      "id": "show_title",
      "default": true,
      "label": "Mostrar título"
    },
    {
      "type": "checkbox",
      "id": "show_price",
      "default": true,
      "label": "Mostrar preço"
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_vendor.label"
    },
    {
      "type": "checkbox",
      "id": "show_countdown",
      "default": false,
      "label": "Mostrar contagem regressiva",
      "info": "Para exibir contagem regressiva, ative está opção. [Saiba mais](https://themihub.gitbook.io/suruchi-theme/collections-and-products/products/add-a-countdown-timer-to-the-product)."
    },
    {
      "type": "checkbox",
      "id": "show_product_rating",
      "default": false,
      "label": "Mostrar avaliação do produto"
    },
    {
      "type": "checkbox",
      "id": "inventory_status",
      "label": "Mostrar status do estoque",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "product_card_radius",
      "label": "Cantos arredondados",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "product_card_spacing",
      "label": "Espaçamento do card",
      "default": false
    },
    {
      "type": "color_scheme",
      "id": "product_card_color_scheme",
      "label": "Esquema de cores do card de produto",
      "default": "background-1"
    },
    {
      "type": "header",
      "content": "Card horizontal"
    },
    {
      "type": "text",
      "id": "horizontal_free_shipping_threshold",
      "label": "Preço mínimo para frete grátis",
      "default": "79,90"
    },
    {
      "type": "text",
      "id": "horizontal_free_shipping_label",
      "label": "Texto do frete grátis",
      "default": "Frete Grátis"
    },
    {
      "type": "text",
      "id": "horizontal_sale_badge_label",
      "label": "Texto do selo de promoção",
      "default": "Promoção"
    },
    {
      "type": "header",
      "content": "Espaçamento da seção"
    },
    {
      "type": "paragraph",
      "content": "Desktop"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Espaçamento superior",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Espaçamento inferior",
      "default": 0
    },
    {
      "type": "paragraph",
      "content": "Mobile"
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Espaçamento superior",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Espaçamento inferior",
      "default": 0
    }
  ],
  "blocks": [
    {
      "type": "product_tab",
      "name": "Coleção",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "default": "Tab",
          "label": "Título"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Coleção"
        },
        {
          "type": "range",
          "id": "products_to_show",
          "min": 2,
          "max": 50,
          "step": 1,
          "default": 8,
          "label": "t:sections.featured-collection.settings.products_to_show.label"
        },
        {
          "type": "header",
          "content": "Configurações do botão"
        },
        {
          "type": "checkbox",
          "id": "show_view_all",
          "default": true,
          "label": "t:sections.featured-collection.settings.show_view_all.label"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Texto do botão",
          "default": "View all"
        },
        {
          "type": "checkbox",
          "id": "button_icon",
          "label": "Usar ícone de seta",
          "default": true
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Estilo do botão",
          "default": "primary",
          "options": [
            {
              "value": "secondary",
              "label": "Secundário"
            },
            {
              "value": "primary",
              "label": "Primário"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_size",
          "label": "Tamanho do botão",
          "default": "medium",
          "options": [
            {
              "value": "large",
              "label": "Grande"
            },
            {
              "value": "medium",
              "label": "Médio"
            },
            {
              "value": "small",
              "label": "Pequeno"
            }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection tabs",
      "blocks": [
        {
          "type": "product_tab",
          "settings": {
            "title": "Tab 1"
          }
        },
        {
          "type": "product_tab",
          "settings": {
            "title": "Tab 2"
          }
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  }
}
{% endschema %}
